/**
 * ──────────────────────────────────────────────
 *  Pi Web Access v2 — Extension Entry
 * ──────────────────────────────────────────────
 * This is the root entry point loaded by the Pi
 * runtime.  It delegates to the structured
 * implementation in `src/`.
 *
 * @module root
 */

import main from "./src/index.js";

export default main;
